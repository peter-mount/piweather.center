\subsectionfit{Station}
\index{Station}
\index{Config!station}

\begin{lstlisting}[language=Go,style=kaolstplain,escapechar=|]
station( "name"
    location( "name" "latitude" "longitude" "altitude" )
    [timezone( "timezone" )]

    // For .calc files
    [calculate from |\dots|]
    [calculate(|\dots|)]
    [ephemeris(|\dots|)]

    // For .station files
    [dashboard(|\dots|)]

    // For .sensor files
    [sensor(|\dots|)]

    // For .cron files
    [tasks(|\dots|)]
)
\end{lstlisting}

\Statement{station} is the base statement within configuration and defines both the station's name and location on the
Earth as well as a container for configuration specific to that station.